/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {

    /* fonte de imagen */
    domains: [
      'source.unsplash.com',
      'wallpaperaccess.com'
    ]
  }
}

module.exports = nextConfig
